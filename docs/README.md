# API Documentation

This repository uses JSDoc for consistent inline documentation and API docs generation.

## Generate docs

From the repository root:

```bash
npm run docs:generate
```

## Output

Generated HTML is written to:

- `docs/api/backend`
- `docs/api/frontend`

Open either `index.html` in a browser to browse the exported documentation.
